## Usage
- npm install
- npm run start

## Features
- Three mode to upload files
    * upload buffer
    * upload formdata
    * use iframe with not refresh
- Compress image 
    * compressFile.html
    * interface `/uploadWithFile`
